{
  "name": "@signageos/nec-sdk",
  "version": "0.0.0",
  "main": "./dist/NECPD",
  "files": [
    "dist",
    "README.md",
    "package.json"
  ],
  "repository": {
    "url": "git@gitlab.com:signageos/nec-sdk",
    "type": "git"
  },
  "scripts": {
    "build": "tsc",
    "lint": "tslint --config node_modules/hugport-codestyle/tslint.json {src,tests}/**/*.{ts,tsx}",
    "prebuild": "rm -rf dist/*",
    "prepublish": "npm run prebuild && npm run build",
    "watch": "tsc --watch"
  },
  "author": "signageOS.io <dev@signageos.io>",
  "license": "MIT",
  "engines": {
    "node": "^8.9.0",
    "npm": "5.3.0"
  },
  "publishConfig": {
    "registry": "https://npm.signageos.io",
    "access": "public"
  },
  "dependencies": {
    "async-lock": "^1.2.2",
    "circular-json": "^0.5.9",
    "serialport": "^8.0.5"
  },
  "devDependencies": {
    "@types/async-lock": "^1.1.1",
    "@types/node": "^12.12.14",
    "@types/serialport": "^7.0.4",
    "tslint": "^5.20.1",
    "typescript": "^3.7.3"
  }
}
